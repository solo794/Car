<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Show every installed app in the App Drawer -->
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission" />

    <!-- Floating / Picture-in-Picture style mini windows drawn over other apps -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <!-- Keep the floating tools + reverse-camera watcher alive -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.REORDER_TASKS" />

    <!-- Quick panel: brightness / volume / connectivity -->
    <uses-permission android:name="android.permission.WRITE_SETTINGS" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.BLUETOOTH"
        android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN"
        android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".LauncherApp"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher_foreground"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_launcher_foreground"
        android:supportsRtl="true"
        android:theme="@style/Theme.ShineLauncher"
        tools:targetApi="34">

        <!-- Single-activity app: Home, App Drawer, Quick Panel and Settings
             all live inside MainActivity via Compose Navigation, so the
             launcher never shows a task-switch flicker between its own
             screens. -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:screenOrientation="portrait"
            android:stateNotNeeded="true"
            android:resizeableActivity="true"
            android:theme="@style/Theme.ShineLauncher"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.HOME" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Floating windows: reverse-camera preview, mini music player,
             dashboard clock. Runs as a foreground service so the overlay
             survives while the driver is inside another app (this is the
             "Picture-in-Picture" feature). -->
        <service
            android:name=".service.FloatingPipService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="floating_pip_overlay_for_car_launcher" />
        </service>

        <!-- Steering-wheel media button handling (play/pause/next/prev) -->
        <service
            android:name=".service.MediaKeyService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="steering_wheel_media_keys" />
        </service>

        <!-- Notification badges on app icons -->
        <service
            android:name=".service.NotificationBadgeService"
            android:exported="true"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <!-- Re-launch the home screen / restore floating tools after the
             head unit reboots. -->
        <receiver
            android:name=".service.BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- Reverse-gear watcher: shows the reverse-camera floating window.
             Head units broadcast different action strings for "reverse
             engaged"; the common ones are registered here and the exact
             one can be added in Settings -> Reverse Camera if this unit
             uses a custom action. -->
        <receiver
            android:name=".service.ReverseCameraReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.REVERSE" />
                <action android:name="android.car.intent.action.REVERSE" />
                <action android:name="com.android.car.reverse" />
                <action android:name="com.reverse.trigger" />
                <action android:name="android.intent.action.CAR_REVERSE_ON" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
